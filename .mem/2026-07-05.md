---
branch: main
focus: adaptive threshold op added + verified; roadmap #4 done
status: wip
---
## next
- [picker] `pick` command using iterfzf (bundles fzf binary, pip-installable)
- [feature] next backlog technique: canny / contours / distance-transform / pixel-sort (see roadmap.md)
- [deploy] install shim on the OTHER machine when used there (`uv tool install --editable .`)

## files
- /Users/briangildea/things/myc/image-wormhole/src/image_wormhole/ops/adaptive.py (adaptive threshold; block×C grid sweep)
- /Users/briangildea/things/myc/image-wormhole/src/image_wormhole/image_io.py (ensure_readable; HEIC→PNG via sips; IMAGE_EXTS)
- /Users/briangildea/things/myc/image-wormhole/src/image_wormhole/cli.py (argparse entry; register subcommands here)
- /Users/briangildea/things/myc/image-wormhole/pyproject.toml (deps + `iw` script)

## done
- [feature] adaptive threshold: `iw adaptive <img>` → grid over --block (odd,>=3; even bumped up) × -C/--const; --method gaussian|mean, --invert. Tag: <method>_b###_c##(m=neg)(_inv). Verified: 15-var default grid, even-block coercion 8→9, neg C -5→cm05, binary [0,255], recovers micro-texture under lighting gradient where global t128 skews. Roadmap #4 DONE.
- [heic] implicit HEIC conversion DONE: threshold + extract route src through image_io.ensure_readable() before imread. Verified end-to-end (synth grad.heic → binary [0 255] output, correct stem naming, extract folder next to input).
- [rename] binary `wormhole`→`iw` and default out dir `output/`→`iw/` (2026-07-05). Reinstalled shim (--force); old wormhole shim gone, `iw` on PATH. README + long.md updated.
- [deploy] `iw` shim installed on this machine via `uv tool install --editable .`; verified on PATH.
- [feature] B/W extractor: `iw extract <img|dir> --keep black|white|both` → transparent RGBA PNGs, sibling extract/ folder.
- [feature] threshold sweep: `iw threshold <img>` → N binary variants, iw/<stem>/threshold/<stem>_t###.png.
- [arch] op module pattern: ops/<technique>.py exposes add_parser(sub)+run(args); cli.py registers; paths.py holds variant_path()/technique_dir().
