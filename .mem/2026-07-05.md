---
branch: main
focus: scaffold done; next feature is threshold sweep
status: wip
---
## wip
- [feature] B/W extractor: isolate only black OR only white from a thresholded output

## next
- [io] folder-structure organization: extend layout as more techniques land
- [picker] `pick` command using iterfzf (bundles fzf binary, pip-installable)

## done
- [feature] threshold sweep DONE: `wormhole threshold <img>` → N binary variants (default 20, endpoints excluded), layout output/<stem>/threshold/<stem>_t###.png. Verified end-to-end (binary output, correct direction).
- [arch] op module pattern: src/image_wormhole/ops/<technique>.py exposes add_parser(sub)+run(args); cli.py registers. paths.py holds variant_path()/technique_dir().

## files
- /Users/briangildea/things/myc/image-wormhole/src/image_wormhole/cli.py (argparse entry; register subcommands here)
- /Users/briangildea/things/myc/image-wormhole/pyproject.toml (deps + `wormhole` script)

## done
- [setup] git init (branch main) + Python 3.12 uv project scaffolded, deps installed (numpy, opencv-python, pillow), `wormhole` CLI verified
